name: lakehouse
version: "1.0.0"
config-version: 2
profile: lakehouse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target

models:
  lakehouse:
    staging:
      +materialized: view
    silver:
      +materialized: incremental
      +schema: dbt_silver
    gold:
      +materialized: table
      +schema: dbt_gold
